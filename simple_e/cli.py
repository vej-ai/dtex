"""Command-line entry point for simpl.E.

This is a skeleton stub. The real CLI surface (``init``, ``new connector``,
``list``, ``test``, ``run``, ``state``) is built in a later stage and will be
a thin shell over the importable library.
"""

import sys


def main() -> None:
    """Entry point for the ``simple-e`` console script (skeleton stub)."""
    print("simple-e (skeleton)")
    sys.exit(0)


if __name__ == "__main__":
    main()
